import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
      flex: 2,
      backgroundColor: '#ccc',
      alignItems: 'center',
      justifyContent: 'center',
      padding: 20
    },
    FontG:{
      fontSize: 32,
    },
    FontDestaque: {
      color: '#F00'
    }
  });